# ====== Global settings ======
# Change these for class/lab usage
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=energy

# MinIO credentials (match docker-compose)
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=changeme123
MINIO_BUCKET=datalake

# Trino
TRINO_PORT=8080

# Mongo
MONGO_INITDB_DATABASE=energy

# DynamoDB (local)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=localstack
AWS_SECRET_ACCESS_KEY=localstack

# ====== XM / SiMEM Ingest Settings ======
# Default SIMEM public API endpoint. Adjust if portal changes.
SIMEM_BASE=https://www.simem.co/backend-files/api/PublicData

# Example dataset id (you MUST verify in SiMEM portal)
# e.g., DEMAND dataset (example placeholder): e007fb
SIMEM_DATASET_ID=e007fb

# Date range for ingest demo (yyyy-mm-dd)
START_DATE=2024-01-01
END_DATE=2024-01-07

# Where to write raw parquet files
RAW_DIR=./data/raw

# Target names for DB loads
PG_SCHEMA=energy
PG_TABLE=demanda_horaria
TS_TIME_COLUMN=fecha  # set the time column name present in your dataset
